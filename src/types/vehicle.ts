export type Vehicle = {
  avatar?: string;
  Modal: string;
  Milage: string;
  emailId: string;
  year: number;
  Make: String;
  id: number;
};

export type VehicleState = {
  vehicleList: Vehicle[];
};
